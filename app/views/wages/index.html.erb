<h1>Wages#index</h1>
<p>Find me in app/views/wages/index.html.erb</p>

<%= link_to '報酬条件入力', new_wage_path %>

<table class="">
<tbody>
<tr>
  <td>名前</td>
  <td>単価</td>
  <td>単位</td>
  <td>編集内容</td>
</tr>

<% @wages.each do |w| %>
    <tr>
    <td><%= w.user.name %></td>
    <td><%= w.unit_price %></td>
    <td><%= w.unit %></td>

    <td><%= link_to "編集", edit_wage_path(id: w.id) %>
        <%= link_to "削除", wage_path(id: w.id), method: :delete , data: {confirm: "削除しますか？"}%></td>

    </tr>
<% end %>
</tbody>
</table>


<div>
<%= link_to "Back", :back %>
</div>